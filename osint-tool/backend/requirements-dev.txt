# Development / CI dependencies. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Tests
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
respx==0.21.1                 # httpx mocking: 8 of the modules talk HTTP
schemathesis==3.36.0          # property-based tests against the generated OpenAPI schema

# Lint / types / security
ruff==0.6.9                   # linter + formatter (replaces flake8 + isort + black)
mypy==1.11.2
bandit==1.7.10                # static security analysis
pip-audit==2.7.3              # known vulnerabilities in pinned dependencies
