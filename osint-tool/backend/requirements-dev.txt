# Development / CI dependencies. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Tests
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
respx==0.23.1                 # httpx mocking: 8 of the modules talk HTTP
schemathesis==4.24.3          # property-based tests against the generated OpenAPI schema

# Lint / types / security
ruff==0.16.0                   # linter + formatter (replaces flake8 + isort + black)
mypy==2.3.0
bandit==1.9.4                # static security analysis
pip-audit==2.10.1              # known vulnerabilities in pinned dependencies
